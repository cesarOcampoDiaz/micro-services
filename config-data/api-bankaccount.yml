management:
  endpoints:
    web:
      exposure:
        include: *
  endpoint:
    shutdown:
      enabled: true
spring:
  application:
    name: api-bankaccounts
  data:
    mongodb:
      database: dbBankAccount
      port: 27017
      host: localhost
server:
  port: 8082
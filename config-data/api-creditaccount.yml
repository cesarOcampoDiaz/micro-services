management:
  endpoints:
    web:
      exposure:
        include: *
  endpoint:
    shutdown:
      enabled: true
spring:
  application:
    name: api-creditaccount
  data:
    mongodb:
      database: dbCreditAccount
      port: 27017
      host: localhost
server:
  port: 8084

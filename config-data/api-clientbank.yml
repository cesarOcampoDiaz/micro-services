management:
  endpoints:
    web:
      exposure:
        include: *
  endpoint:
    shutdown:
      enabled: true
spring:
  application:
    name: api-clientbank
  data:
    mongodb:
      database: dbClientBank
      port: 27017
      host: localhost
server:
  port: 8081
  

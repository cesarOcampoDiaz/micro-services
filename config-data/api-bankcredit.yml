management:
  endpoints:
    web:
      exposure:
        include: *
  endpoint:
    shutdown:
      enabled: true
spring:
  application:
    name: api-bankcredit
  data:
    mongodb:
      database: dbBankCredit
      port: 27017
      host: localhost
server:
  port: 8003
